{
  "name": "christianmacion-ai-proxy",
  "version": "1.0.0",
  "description": "Edge AI proxy for christianmacion.github.io. v1.0.0 — typed contract, origin check, KV rate limit, Workers AI (llama-3.1-8b-instruct), error envelope.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
